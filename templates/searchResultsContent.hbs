{{#if isBlank}}
{{else}}
{{#if isAwaitingResults}}
    <div class="no-results" aria-label="{{{awaitingResultsMessage}}}">
        <div class="search-result-inner">
            {{{awaitingResultsMessage}}}
        </div>
    </div>
{{else}}
    {{#each formattedResults}}
        <button class="search-result" data-id="{{id}}" aria-label="{{{searchTitle}}}. {{{textPreview}}}" role="listitem">
            <div class="search-result-inner">
                <div class="drawer-item-title result-title">{{#if ../_showHighlights}}{{{searchTitleTagged}}}{{else}}{{{searchTitle}}}{{/if}}</div>
                {{#if textPreview}}
                    <div class="result-preview">"{{#if ../_showHighlights}}{{{textPreviewTagged}}}{{else}}{{{textPreview}}}{{/if}}"</div>
                {{/if}}
                {{#if ../_showFoundWords}}
                <div class="result-foundwords"><i>{{{foundWords}}}</i></div>
                {{/if}}
            </div>
        </button>
    {{else}}
         <div class="no-results" aria-label="{{{noResultsMessage}}}">
            <div class="search-result-inner">
                {{{noResultsMessage}}}
            </div>
        </div>
    {{/each}}
{{/if}}
{{/if}}
